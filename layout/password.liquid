<!doctype html>
<html lang="{{ request.locale.iso_code }}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>{{ shop.name }}</title>
  {{ content_for_header }}
  <style>
    body { margin: 0; background: #007e40; color: #222; }
  </style>
</head>
<body>
  {{ content_for_layout }}
</body>
</html>